---
interface Props {
    title: string;
    text: string;
}

const { title, text } = Astro.props;
---

<section class="py-10 text-center">
    <h1 class="text-4xl font-bold">{title}</h1>
    <p class="pt-2">{text}</p>
</section>
